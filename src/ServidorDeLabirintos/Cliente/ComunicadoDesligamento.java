package ServidorDeLabirintos.Cliente;

public class ComunicadoDesligamento {
    // Mensagem para todos clientes, notificando que o servidor está encerrando (MENSAGEM DO SERVIDOR).
    // (No servidor) -> Desativar -> é criado e instaciado objeto dessa classe -> mandado para todos clientes conectados
}
